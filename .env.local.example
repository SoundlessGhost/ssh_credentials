# Copy to .env.local and edit. .env.local is gitignored by Next.js default.
# NEXT_PUBLIC_* values are baked at build time into the JS bundle.

# Backend API base URL. No trailing slash.
NEXT_PUBLIC_API_URL=http://localhost:8080
