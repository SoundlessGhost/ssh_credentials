// Terminal hook scaffold. Step 11 implements xterm.js connection to
// ws://.../ws/terminal/{session_id} with proper resize + tab support.
// Intentional placeholder so imports compile before the real component lands.

export function useTerminal(_sessionId: string | null) {
  return null;
}
